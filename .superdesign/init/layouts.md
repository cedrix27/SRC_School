# Shared layouts

`apps/web/app/layout.tsx` provides the root App Router layout, metadata, French language and DM Sans imports. `apps/web/app/page.tsx` contains the responsive sidebar, top bar and main shell.
