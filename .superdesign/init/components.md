# Shared components

No dedicated component library yet. The dashboard is implemented in `apps/web/app/page.tsx` with inline React components (`Dashboard`, `Stat`, `Collection`, `CreateForm`) and CSS tokens in `apps/web/app/globals.css`.
